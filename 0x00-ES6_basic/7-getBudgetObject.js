/**
 * Shorthand syntax: keynames that represent object property value
 * getBudgetObject - Object shorthand syntax
 */
export default function getBudgetObject(income, gdp, capita) {
  const budget = {
    income,
    gdp,
    capita,
  };

  return budget;
}
